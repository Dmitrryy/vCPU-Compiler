
#ifndef STACK_2_STACK_H
#define STACK_2_STACK_H

#include "../../source/repos/CPU/error.h"

///?????? ????????
#define PAGE_SIZE 4096

///????????? ??????
#define DISABLE_CHECK_PROTECT

#include <cstddef>
#include <iostream>
#include <cctype>
#include <functional>
#include <ctime>
#include <windows.h>
#include <fstream>
#include <cmath>
#include <csignal>
#include <iomanip>

/**
 * ????? ?? ?????? ? Stack
 *
 * @tparam T - ??? ??????
 * @param m_data - ????????? ?? ?????? ???????
 * @param m_size - ???? ???-?? ????????? ? Stack
 * @param m_n_now - ???-?? ??????? ? Stack ?????????
 * @param m_nom_page - ???-?? ??????? ? Stack
 */
template <typename T>
class Stack {
private: T* m_data = NULL;
private: int m_size = 0;
private: int m_n_now = 0;
private: int m_nom_page = 0;

public: void OK(int stat);

public: void Dump();

public: void Create(int size);
public: void Destroy();
public: void Push(T value);
public: T Pop();
};


#endif //STACK_2_STACK_H
